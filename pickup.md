# Distilleri - Where I left off

## Done
- manifest.json (MV3, permissions)
- background.js (worker: storage, messaging, summarization)
- content.js (injector: readability, DOM, highlights, tooltips)
- popup.html + popup.css (selection UI)
- options.html + options.js (settings: keys, tier toggle, model select)

## Left
- styles.css needs options page styles (inputs, toggles, buttons, sections, dropdown, upsell)
- Add highlight color scale (pink→red) for opinions
- Add fallacy tooltip/side panel styles
- Test extension loading